// 'Presenter': Det som faktiskt visas i appen

import WakeupConfigView from "../views/WakeupConfigView";

function WakeupConfigPresenter(props) {
    return (
        <WakeupConfigView />
    );
}

export default WakeupConfigPresenter;